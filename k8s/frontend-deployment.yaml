apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      serviceAccountName: default
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: frontend
        # IMPORTANT: Replace this with the actual path to your container image
1        image: ghcr.io/tomknightatl/usccb/usccb-frontend:v1.0.1
        ports:
        - containerPort: 80
