Version without secrets:
# GitHub Repo
GITHUB_REPO=<url>

# GitHub Personal Access Token
# Note: GH_TOKEN and GITHUB_TOKEN should have the same value (your PAT)
# Some tools use GH_TOKEN, others use GITHUB_TOKEN, so we set both
GH_TOKEN=<secret>
GITHUB_TOKEN=<secret>

# GitHub Username
# Note: When using GitHub Container Registry (ghcr.io), DOCKER_USERNAME 
# is always the same as your GITHUB_USERNAME
GITHUB_USERNAME=<secret>
DOCKER_USERNAME=<secret>

# Supabase Secrets
SUPABASE_KEY=<secret>
SUPABASE_URL=<url>

# AI Inference API Keys:
GENAI_API_KEY_USCCB=<secret>
SEARCH_API_KEY_USCCB=<secret>
SEARCH_CX_USCCB=<secret>
OpenAIAPIKeyforUSCCBKey=<secret>